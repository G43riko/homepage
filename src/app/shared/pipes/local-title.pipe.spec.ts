import { LocalTitlePipe } from './local-title.pipe';

describe('LocalTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
