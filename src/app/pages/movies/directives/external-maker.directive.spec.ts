import { ExternalMakerDirective } from "./external-maker.directive";

describe("ExternalMakerDirective", () => {
    it("should create an instance", () => {
        const directive = new ExternalMakerDirective();
        expect(directive).toBeTruthy();
    });
});
